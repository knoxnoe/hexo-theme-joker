//- extends includes/layout.pug

//- block content
//-   if page.type === 'categories'
//-     h1= page.type
//-     #post-content
//-       .category-lists
//-         .category-title= _p('page.category')
//-         span.category-line -
//-         span.category-amount= site.categories.length
//-         div!= list_categories()
//-   else if page.type === 'about'
//-     h1= page.type
//-     .tag-cloud
//-       .tag-cloud-title= _p('menu.tags')
//-       span.tag-cloud-line -
//-       span.tag-cloud-amount= site.tags.length
//-   else
//-     article#page
//-       h2= page.title
//-       h2= page.type
//-       != page.content
//-   #paginator!= paginator()
